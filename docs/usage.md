# 運用ガイド

## データ入力

### スプレッドシートから入力

`P/L` シートに直接データを追加できます。

| 月 | カテゴリ | 金額 |
|----|---------|------|
| 2025-01 | 給与 | 250000 |
| 2025-01 | 食費 | -3500 |
| 2025-01 | 交通費 | -500 |

**注意**: 金額は正の値で入力し、支出の場合は負の値として記録されます。

### フロントエンドから入力

ヘッダー右上の「入力」ボタンをクリックすると、モーダルフォームが表示されます。

#### 選択可能な月の制限

- 開始月（B/Sシートで設定）から先々月（今から2ヶ月前）までの範囲
- 既にデータが存在する月は選択不可（重複登録を防止）
- 先月・今月は選択不可（確定していないため）

#### 入力方法

1. ヘッダー右上の「入力」ボタンをクリック（選択可能な月がない場合は非表示）
2. モーダルで月を選択（ドロップダウン）
3. 各カテゴリの金額を入力（支出・収入それぞれ、初期値は0、必須項目）
4. 残高を入力（初期値は0、必須項目）
5. 「まとめて追加」ボタンをクリック
6. 追加後、グラフは自動的に更新され、モーダルが閉じます

#### 特徴

- 複数のカテゴリを一度に入力可能（一括登録）
- 既存カテゴリが自動的に表示され、各カテゴリの金額を個別に入力
- すべての入力フィールドは必須項目で、初期値は0
- 0円のレコードも追加可能
- 月ごとの残高も同時に登録・更新可能
- 追加後、グラフは自動的に更新されます

## カテゴリの追加

マスタ管理は不要です。カテゴリはP/Lシートに初めて登録された順序で表示されます（アルファベット順ではありません）。

- 新規カテゴリはP/Lシートに直接追加するか、フロントエンドから新しいカテゴリ名で登録することで自動的に追加されます
- 追加したカテゴリは自動的にグラフに反映され、次回以降は入力フォームに表示されます
- カテゴリの順序はスプレッドシートでの出現順が保持されます

## データの編集・削除

データの編集・削除はスプレッドシートで直接行います。フロントエンドからは追加のみ可能です。

## GASの再デプロイ

GASのコードを更新した場合は、新しいバージョンとしてデプロイが必要です。

1. Apps Script エディタで「デプロイ → デプロイを管理」を選択
1. 鉛筆アイコンをクリック
1. バージョンで「新しいバージョン」を選択
1. 「デプロイ」をクリック

※ URLは変わりません

## グラフの表示

### 総資産推移グラフ

- B/Sシートに残高が設定されている月はその残高を直接表示
- 残高が設定されていない月は、初期残高と累積収支から自動計算

### 収支・利益グラフ

- 収入: 正の棒グラフ
- 支出: 負の棒グラフ
- 利益: 折れ線グラフ

### カテゴリ別支出グラフ

- 支出のみをカテゴリごとに色分けした積み上げ面グラフで表示
- カテゴリはスプレッドシートでの出現順で表示

### 表示期間の切り替え

- 月別表示: デフォルトで直近12ヶ月を表示
- 年別表示: デフォルトで直近5年を表示
- ボタンで切り替え可能
